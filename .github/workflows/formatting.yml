name: "Formatting"

on: [push]
jobs:
  black:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        python-version: ["3.12"]
        os: [ubuntu-latest]
        tool: ["black", "isort"]
    steps:
      - uses: actions/checkout@v4
      - name: Install uv
        uses: astral-sh/setup-uv@v9.0.0
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: uv sync --group formatting
      - name: ${{ matrix.tool }} Code Formatter
        run: uv run --group formatting ${{ matrix.tool }} . --check
